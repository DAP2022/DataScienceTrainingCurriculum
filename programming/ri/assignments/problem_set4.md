Hand in a .qmd file saved as “FirstInitialLastname_Day4.qmd” that has markdown explanations and coding chunks (with hashed out comments).
## Question 1:
__USE BASE R to do the following (you will redo the same question using ggplot2 for Intro to RII Module 3 Assignment).__

## Question 1:
With the bumpus dataset choose a numeric trait (one of the columns such as head, humer, wgt etc):

- Using **two different types of graphs**, visually inspect to determine if your chosen variable is normally distributed. (Hint: a histogam AND one other type are expected). 
- Graph a boxplot for your chosen variable that is divided/grouped by BOTH sex and survival. (four groups: female survived, female died, male survived, male died). Describe in a Markdown chunk what the boxplot suggests about sex difference and survival difference in this trait. 
- Draw a mosaic plot of the survival of the female versus male birds. Described what conditional probability the mosaic plot is communicating. Does sex appear to be associated with survival?
- Identify the interquartile range (IQR) of your chosen trait, separately for survivors and non-survivors, using the IQR() function. In Markdown, explain what the IQR teslls you that the mean alone does not. 

## Question 2: 
Use the built in data set, iris (or mtcars, if you prefer. Note: you will revisit this dataset in RII, module 3): 
- Create a scatterplot of two numeric variables that you hypothesize might be related. Justify your choice.
- Add color to distinguish a categorical grouping variable. What does the color-coding reveal about the relationship?  
